# Phase 21 Plan - Arc Two Korean-Primary Start

## Goal

Start the next QFUDS SAGA prose loop with a concise Arc Two plan and the first
Korean-primary draft after the first arc.

## Tasks

1. Add Arc Two planning document under `10_story_design/`.
2. Add Episode 025 Korean-primary prose draft under `20_drafts/`.
3. Update SAGA read order and wiki index.
4. Validate frontmatter, links, workflow guard, and repo preflight.
5. Commit, fast-forward merge to `codex/qfuds-saga-fiction-archive`, and push.

## Acceptance Criteria

- The new plan answers what Arc Two is about without overdesigning the whole arc.
- The 025 draft begins from `who may author loss` and turns it into an embodied
  scene, not an abstract legal essay.
- The draft follows the Korean-primary prose rule and the term-discipline
  glossary.
- The draft contains a `Harness Applied` block and `Continuity Notes`.
- No new external source/product claim is introduced.

## Verification

Run:

```bash
python3 scripts/validate_docs.py
python3 scripts/research_consistency.py
git diff --check
python3 scripts/agent_workflow_guard.py --staged
make preflight
sh scripts/git-hooks/pre-commit
```
